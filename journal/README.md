# Journal

Trade notes written by `trade-journal-postmortem` (one file per trade,
`<TICKER>-<YYYY-MM-DD>.md`, schema in that skill). This folder is the system's persistent
memory for scheduled/cloud runs — see `workflows/daily-automation.md`. If you use Obsidian
locally, point the vault (or a sync job) at this folder.
